name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s "https://fonouaperojtxczgorqh.supabase.co/rest/v1/" --max-time 10 || true
          echo "Ping OK"
